-- vim.keymap.set("x", "<gt>", function()
-- 	print("> pressed")
-- end, {})

return {
	"bullets-vim/bullets.vim",
	-- config = function()
	-- 	-- This way, bullet points are extended with bullet points when indenting, while numbered lists are also extended with bullet points
	-- 	vim.g.bullets_outline_levels = { "num", "std-" }
	-- 	vim.g.bullets_set_mappings = 0
	-- end,
}
